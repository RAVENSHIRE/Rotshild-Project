// ------------------------------------------------------------------
// Firebase web configuration.
// Paste your project's config from:
//   Firebase console → Project settings → Your apps → SDK setup (Config)
//
// While the placeholders below are unchanged, the app runs in DEMO
// mode: authentication and portfolio persistence are simulated with
// localStorage so every page stays fully interactive.
// ------------------------------------------------------------------
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
